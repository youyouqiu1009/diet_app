# アチーブメント機能の反映

1. このアプリの Supabase プロジェクトで **SQL Editor** を開きます。
2. `supabase_step10.sql` の内容を貼り付け、**Run** を押します。
3. アプリの変更を GitHub に push し、GitHub Pages の更新後に再読み込みします。

既存の体重・カロリー・設定はそのままで、達成履歴の保存先だけ追加します。
SQL は繰り返し実行できます。ログインした本人の履歴だけを読み書きできます。

目標達成ラインは、基準体重から基準日以降、昨日までの累積カロリー収支を反映した理論体重で計算します。
消費カロリーの推定方法はこれまでと同じです。カロリー未記録の日は理論体重を変化させません。
達成度も同じ基準日から昨日までで計算し、丸める前の理論体重が目標体重以下になった日を達成日として保存します。
今日のカロリー入力は、達成度・理論体重・アチーブメントの判定・マイナスカロリーの総量に含めません。
グラフの今日の理論体重は昨日と同じ暫定値で表示します。基準日が今日の場合は基準体重を表示します。
日付が変わった後の再読み込みで、前日分の記録が計算に含まれるようになります。

保存済みのアチーブメントは、目標・基準の変更や記録の削除後も残ります。
初回読み込みでは、現在設定中の目標について過去の記録から達成日を確認します。
導入前に別の目標へ変更済みの場合、その古い目標は保存されていないため復元できません。
基準日・基準体重・目標体重・目標日が同じものは、同じ目標として重複保存しません。

計算と保存処理の回帰テストは `node tests/goals.test.js` で実行できます（追加パッケージ不要）。
